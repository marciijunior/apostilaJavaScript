const soma = (a,b) => a + b;
console.log(soma(5,7));
// Neste console.log, vemos primeiro a chamada da funcao, atraves do termo "soma", logo após, passamos os argumentos que serão atribuido a função.
// In this console.log, we first see the function call, through the term "soma", then we pass the arguments that will be assigned to the function.