const palavra = "Olá";

for (const letra of palavra) {
  console.log(letra);
}
// Por não termos aberto um array, isso é executado como se cada letra fosse algo a ser lido.
// Since we haven't opened an array, this is executed as if each letter were something to be read.